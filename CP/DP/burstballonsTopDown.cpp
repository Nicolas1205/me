#include<bits/stdc++.h>

using namespace std; 

int dp[MAXN][MAXN];

// such thart dp[i][j ] where i represent the ballons bursted until now , and j the best value at the moment for that combination i_1 , i_2  , i_n ballons bursted. 

int solve(vector<int> &a){
  if() {
    return dp; 
  }
  
  for(int i =0 ; a.size() ; i++){ 
     
  }
  return ans; 

}

int main(){
  int tt; 
  while(tt-- ){
    int n ; cin>>n;
    vector<int> a(n); 
    for(int i=0 ;i   < n ; i++) cin>>a[i] ;
    cout<<solve(a);
  }
  
}
